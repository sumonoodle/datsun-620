# Research scaffolding: fetch real search pages from the Phase A/B Asian
# sources so collector parsers are written against real markup. Runs when
# the fetch script changes on the dev branch; commits gzipped pages back.
# Deleted once the collectors ship.
name: fetch-asia-pages

on:
  push:
    branches:
      - claude/datsun-620-kickoff-ah5cyw
    paths:
      - scrapers/experiments/asia_fetch.py
      - .github/workflows/fetch-asia-pages.yml

permissions:
  contents: write

concurrency:
  group: fetch-asia-pages
  cancel-in-progress: false

jobs:
  fetch:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r scrapers/requirements.txt

      - name: Fetch pages
        run: python scrapers/experiments/asia_fetch.py

      - name: Commit fetched pages
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/research/pages
          if git diff --cached --quiet; then
            echo "no changes"
            exit 0
          fi
          git commit -m "research: fetched Asia source search pages"
          for i in 1 2 3 4; do
            git push && exit 0
            git pull --rebase || git rebase --abort
            sleep $((2 ** i))
          done
          git push
